{
  "rules": {
    "tasks": {
      "handleliste": {
        ".read":
          "auth !== null && (auth.email === 'martemorland@gmail.com' || auth.email === 'anders@andersos.net')",
        ".write":
          "auth !== null && (auth.email === 'martemorland@gmail.com' || auth.email === 'anders@andersos.net')"
      }
    }
  }
}
